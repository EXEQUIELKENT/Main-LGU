DB_HOST=localhost
DB_NAME=main_lgu
DB_USER=root
DB_PASS=

# Per-system shared secrets used to sign SSO tokens (HMAC-SHA256).
# Must match the SSO_SHARED_SECRET configured on each target system.
SSO_SECRET_ROADMON=
SSO_SECRET_IPMS=
SSO_SECRET_ENERGY=
SSO_SECRET_CPRF=
SSO_SECRET_CIMM=
